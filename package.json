{
  "name": "whatsapp-webhook-api",
  "version": "1.0.0",
  "description": "WhatsApp Business API webhook - receive and send messages",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "nodemon src/index.js",
    "test": "jest",
    "test:watch": "jest --watch",
    "db:truncate": "node scripts/db-reset.js truncate",
    "db:reset": "node scripts/db-reset.js schema",
    "db:reset:full": "node scripts/db-reset.js full"
  },
  "dependencies": {
    "axios": "^1.6.0",
    "dotenv": "^16.3.0",
    "express": "^4.18.0",
    "node-nlp": "^5.0.0-alpha.5",
    "pg": "^8.21.0"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "nodemon": "^3.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
