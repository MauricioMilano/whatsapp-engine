# Server
PORT=3000

# WhatsApp Credentials
VERIFY_TOKEN=your_verify_token_here
ACCESS_TOKEN=your_facebook_access_token_here
PHONE_NUMBER_ID=your_phone_number_id

# Optional - App Secret (for signature verification)
APP_SECRET=your_app_secret_here

# PostgreSQL Configuration (for NLP dialogue context persistence)
# Note: from the host (e.g. running `npm run db:truncate` or psql directly)
# the docker-compose stack exposes Postgres on port 5435, not 5432.
# Inside the compose network the app talks to the service on port 5432
# directly (docker-compose.yml overrides this variable for the app service).
DATABASE_URL=postgresql://botuser:botpass123@localhost:5435/whatsapp_bot

# NLP Dialogue Engine
ENABLE_NLP_DIALOGUE=true
DIALOGUE_PATH=./dialogue.json
SESSION_TIMEOUT_MS=1800000
FALLBACK_SCORE_THRESHOLD=0.7