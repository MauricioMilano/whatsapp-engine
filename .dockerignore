# Dependencies
node_modules/
npm-debug.log*

# Environment files (secrets — pass via env_file in compose)
.env
.env.*
!.env.example

# Tests
tests/
coverage/
*.test.js
jest.config.*

# Dev scripts (not needed in the runtime image)
scripts/

# OS / editor junk
.DS_Store
.vscode/
.idea/
*.log

# Docs & specs (not needed in image)
docs/
openspec/
.github/
examples/

# Docker self-references
Dockerfile
.dockerignore
docker-compose*.yml
model.nlp